en:
  graphql:
    mutations:
      desc: The schema’s entry-point for mutations.
      common:
        fields:
          id: ID
          uuid: UUID
      user:
        sign_up:
          desc: Sign up a user
        sign_in:
          desc: Sign in a user
